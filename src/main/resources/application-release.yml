server:
  port: 8080

spring:
  datasource:
    username: root
    password: aa6638360
    url: jdbc:mysql://localhost:3306/springbootrelease?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost
    port: 6379
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
    timeout: 50

mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.aaron.entity

#showSql
logging:
  level:
    com:
      example:
        mapper: debug